#include <iostream>
using namespace std;

int main()
{

    // we can use any name for variable but we should follow some rules.
    // 1. we can use only alphabets , numbers and underscore(_) in variable name.
    // 2. we can not use any special character in variable name.like !@#%^&*()_
    // 3. we cannot start the variable with numbers.like 1name etc.
    // 4. we cannot use any keyword as variable name.like int,float,char etc.
    // 5. we can start the variable name with only underscore _ or $ and not with any numer.
    // 6. we can also use only _ or $ in variable name.
    // 7. commas or blanks are not allowed in variable name.
}