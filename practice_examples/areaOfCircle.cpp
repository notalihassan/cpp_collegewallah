#include <iostream>
using namespace std;

int main()
{

    float r = 4.3;            // radius
    float a = 3.1415 * r * r; // area of circle = pi * r squared

    cout << "Area of circle is " << a << endl;

    // we can get are of circle just by changing the value of r.
}