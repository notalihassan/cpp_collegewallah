#include <iostream>
using namespace std;

int main()
{
    int n;

    cout << "Enter the number : ";
    cin >> n;

    if (n % 2 == 0)
    {
        cout << "Even" << endl;
    }
    else
    {
        cout << "Odd" << endl;
    }

    // 1. a = b means a is assigned the value of b
    // 2. a == b means a is equal to b
    // 3. a != b means a is not equal to b
}